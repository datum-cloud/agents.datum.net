import { Section } from '../layout/Section';
import { SectionBody } from '../layout/SectionBody';

export function MissionSection() {
  return (
    <Section id="mission" n="02" title="Mission">
      <SectionBody>
        <p>
          Datum&apos;s mission is to help the next 1k clouds by unlocking internet superpowers for
          every agent and builder.
        </p>
      </SectionBody>
    </Section>
  );
}
